// En local (docker compose ou uvicorn), l'API tourne sur localhost:8000.
// A adapter une fois l'API deployee (meme convention que orange-platform/frontend/js/config.js).
export const API_URL = "http://localhost:8000";
